set(_target "ad_msg_bridge")

# Include
list(APPEND ${_target}_INCLUDE_DIRS "${Rscl_PATH}/include/")

# Library
## Need user to select the actual library


# Debug
# message(STATUS "${_target}_INCLUDE_DIRS: ${${_target}_INCLUDE_DIRS}")
# message(STATUS "${_target}_LIBRARIES: ${${_target}_LIBRARIES}")
